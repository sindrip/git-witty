{
	"name": "git-witty",
	"bin": {
		"git-witty": "dist/git-witty"
	},
	"module": "src/index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "bun build ./src/index.ts --compile --outfile dist/git-witty",
		"check": "biome ci --diagnostic-level=info .",
		"format": "biome check --fix .",
		"test": "bun test"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.6",
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"commander": "^14.0.3"
	}
}
